services:
  - type: web
    name: ad-schedule-optimizer
    runtime: python
    plan: free
    rootDir: full-version
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PORT
        value: 10000
